<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" 
  integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>

<body>
  <header>
    <% include ./partials/_header %>
  </header>  
  <div class="container">
  <h1 class="text-center"><%= `ShortURL:  ${shortURL}`%></h1>
  <form action="<%= `/urls/${shortURL}?_method=PUT` %>" method='POST'>
    <input class="center-block" id='longURL' type='text' name='longURL' placeholder="<%= url %>" style='width: 300px'>
    <br>
    <input class=" btn btn-info center-block"type='submit' value='Update'>
  </form>
  <br>
  <% if (linkError !== undefined) { %>
      <p class = "text-center" style = "color:red;"><%= linkError %></p>
  <% } %>
  <br>
  <table class="table table-bordered">
    <tbody>
      <% for(let i = 0; i < visits.length; i++) { %>
        <tr>
          <td>&emsp; <%= visits[i].uniqueID %>&emsp;
          <td>visited on:&emsp;</td>
          <td><%= visits[i].timestamp %> </td>
        </tr>
      <% } %>
    </tbody>
  </table>    
  
  </div>

</body>
</html>